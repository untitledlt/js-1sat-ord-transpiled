{
  "name": "js-1sat-ord-transpiled",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsup src/index.ts --dts --no-external --external esbuild --out-dir dist"
  },
  "packageManager": "yarn@4.5.0",
  "dependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.2"
  },
  "devDependencies": {
    "@swc/core": "1.7.26",
    "tsup": "^8.3.0"
  }
}
